## 👩‍💻 Автор работ
ФИО: Ерина Мария Валерьевна  
Группа: 2ПМ-3

---

# Лабораторная работа №1: Nginx + Docker

### 📌 Описание задания
Создать веб-сервер в Docker с использованием Nginx и подключить HTML-страницу.  
Результат доступен по адресу [http://localhost:8888](http://localhost:8888).

---

### ⚙️ Как запустить проект

1. Клонировать репозиторий:
   ```bash
   git clone <ссылка на репозиторий>
   cd nginx-lab
2. Запустить контейнеры:
   ```bash
   docker-compose up -d --build
   ```
3. Открыть в браузере:
   ```http://localhost:8888```
   
---

📂 Содержимое проекта

```docker-compose.yml``` — описание сервиса Nginx

```code/index.html``` — главная HTML-страница

```screenshots/``` — все скриншоты

📸 [Скриншоты работы](https://github.com/MaryaELF/WB_BFU/tree/main/Lab1/screenshots)

✅ Результат
Сервер в Docker успешно запущен, Nginx отдаёт мою HTML-страницу.

---

# Лабораторная работа №2: HTML JS PHP-FPM

### 📌 Описание задания
Настройка Nginx + PHP-FPM. Основы HTML-форм и обработка на JavaScript.  
Результат доступен по адресу [http://localhost:8080/index.html](http://localhost:8080/index.html).

---

### ⚙️ Как запустить проект

1. Клонировать репозиторий:
   ```bash
   git clone <ссылка на репозиторий>
   cd nginx-lab
2. Запустить контейнеры:
   ```bash
   docker-compose up -d --build
   ```
3. Открыть в браузере:
   ```http://localhost:8080/index.html```

---
   
📂 Содержимое проекта

```docker-compose.yml``` — описание сервиса Nginx

```www/index.html``` — главная HTML-страница

```screenshots/``` — все скриншоты

📸 [Скриншоты работы](https://github.com/MaryaELF/WB_BFU/tree/main/Lab2/screenshots)

✅ Результат
HTML-страница успешно запущена.

---

# Лабораторная работа №3: PHP Sessions Files

### 📌 Описание задания
Используем форму из ЛР-2, но теперь обработка данных будет выполняться на сервере через PHP. Данные сохраняем в сессии, а затем в файл.  
Результат доступен по адресу [http://localhost:8080](http://localhost:8080).

---

### ⚙️ Как запустить проект

1. Клонировать репозиторий:
   ```bash
   git clone <ссылка на репозиторий>
   cd nginx-lab
2. Запустить контейнеры:
   ```bash
   docker-compose up -d --build
   ```
3. Открыть в браузере:
   ```http://localhost:8080```

---
   
📂 Содержимое проекта

```docker-compose.yml``` — описание сервиса Nginx  

```www/index.html``` — главная HTML-страница  

✅ Результат
HTML-страница успешно запущена, сохранение данных и переход по страницам работает.

---

# Лабораторная работа №4: Composer, классы и работа с публичным API Nginx + Docker

### 📌 Описание задания
подключить внешние библиотеки с помощью Composer и интегрировать в свой проект публичный API без регистрации.   
Результат доступен по адресу [http://localhost:8888](http://localhost:8888).

---

### ⚙️ Как запустить проект

1. Клонировать репозиторий:
   ```bash
   git clone <ссылка на репозиторий>
   cd nginx-lab
2. Запустить wsl, а затем и сервер:
   ```bash
   php -S localhost:8000
   ```
3. Открыть в браузере:
   ```http://localhost:8888```
   
---

📂 Содержимое проекта

```vendor/``` — папка с библиотеками  

```index.php``` — главная страница

✅ Результат
Сервер запущен, в браузере высвечивается главная страница
